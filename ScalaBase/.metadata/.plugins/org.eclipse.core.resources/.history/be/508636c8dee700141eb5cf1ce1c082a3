package org.hebut.yu

import java.io.PrintWriter

object index extends App{
/*  val fileName = "src\\index.txt"
    val out = new PrintWriter(fileName)
    for (n <- 0 to 20) out.println(makeStr(n))
    out.close

    def makeStr(n: Int) = {
        val t = math.pow(2, n)
        " " * 10 + math.pow(2, n).toInt + " " * (11-t.toString.size) + 
        math.pow(2,-n) 
    }*/
  val FileName="Index"
  val path="src\\"+FileName+".txt"
  val out=new PrintWriter(path)
  for (i <- 0 to 20)
    out.println(OutIndex(i))
  def OutIndex(n:Int)={
    val value=math.pow(2, n)
    //" "*4+value.toInt+" "*(11-value.toString().size)
    value
  }
}